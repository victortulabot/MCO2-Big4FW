<div class="content-sidenav center">
    <a href="/profile/{{userid}}"><img class="img-circle mx-auto d-block" src="/avatars/{{DisplayPicture}}" /> </a>
    {{!-- <div class="cs_{{_id}}" style="display: none">{{CreditScore}}</div>    --}}
    <div class="rank-label-container">
        <span class="label label-default rank-label cs_{{user._id}}" id="creditScore" title="Credit Score" style="cursor:default;">{{CreditScore}} pts</span>
    </div>
    <p class="p-name text-center">{{fName}} {{lName}}</p>
    <p class="p-name text-center" style="font-size: 13px; margin-top: -20px; font-family:Arial, Helvetica, sans-serif; font-style: italic;" id="DisplayName">@{{DisplayName}}</p>
</div>